steps:
  # 1. Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-east1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/thehiregroundpodcast/thehiregroundpodcast:$COMMIT_SHA'
      - '--build-arg'
      - 'VITE_FIREBASE_API_KEY=${_VITE_FIREBASE_API_KEY}'
      - '.'
  

  # 2. Push the built image to the Google Cloud registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-east1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/thehiregroundpodcast/thehiregroundpodcast:$COMMIT_SHA'

  # 3. Deploy the new image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'thehiregroundpodcast'
      - '--image'
      - 'us-east1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/thehiregroundpodcast/thehiregroundpodcast:$COMMIT_SHA'
      - '--region'
      - 'us-east1'

# Explicitly set logging options for the service account
options:
  logging: CLOUD_LOGGING_ONLY